import { css } from '@styled-system/css';

export const container = css({
  display: 'flex',
  alignItems: 'center',
  gap: 1,
});

export const button = css({
  cursor: 'pointer',
  fontSize: '1rem',
});
