import { css } from '@styled-system/css';

export const button = css({
  backgroundColor: 'transparent',
  width: '1.75rem',
  height: '1.75rem',
});

export const avatar = css({
  alignItems: 'center',
  justifyContent: 'center',
  _hover: { backgroundColor: 'primary/20' },
});
