import { css } from '@styled-system/css';

export const header = css({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'end',
  alignItems: 'center',
  gap: 4,
  width: '100%',
  height: '5rem',
  padding: 4,
});
